using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class TileTypes
{
    public enum Walls
    {
        filled,
        empty
    }

    public enum Appartus
    {
        mirror135,
        mirror45,
        emitter,
        extender,
        mixer
    }

}
